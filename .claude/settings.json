{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json,sys,pathlib,tempfile; d=json.load(sys.stdin); sid=d.get('session_id','unknown'); (pathlib.Path(tempfile.gettempdir()) / f'.claude-devtwin-edited-{sid}').touch()\""
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json,sys,pathlib,tempfile; d=json.load(sys.stdin); sid=d.get('session_id','unknown'); f=pathlib.Path(tempfile.gettempdir())/f'.claude-devtwin-edited-{sid}'; f.exists() and (f.unlink(), print(json.dumps({'decision':'block','reason':'Files were edited this turn. Run /code-review on the changes before finishing.'})))\""
          }
        ]
      }
    ]
  }
}
