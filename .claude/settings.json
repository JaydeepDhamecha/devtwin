{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json,sys,pathlib; d=json.load(sys.stdin); sid=d.get('session_id','unknown'); pathlib.Path(f'/tmp/.claude-devtwin-edited-{sid}').touch()\" 2>/dev/null || true"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import json,sys,pathlib\nd=json.load(sys.stdin)\nsid=d.get('session_id','unknown')\nf=pathlib.Path(f'/tmp/.claude-devtwin-edited-{sid}')\nif f.exists():\n    f.unlink()\n    print(json.dumps({'decision':'block','reason':'Files were edited this turn. Run /code-review on the changes before finishing.'}))\" 2>/dev/null"
          }
        ]
      }
    ]
  }
}
