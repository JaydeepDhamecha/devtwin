[tool.poetry]
name = "example-poetry-app"
version = "0.1.0"
description = "fixture project"

[tool.poetry.dependencies]
python = "^3.11"
requests = "^2.31"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
