{
  "name": "@mcphub/plugin-devtwin",
  "version": "1.0.0",
  "description": "Live, structured view of a developer's local environment: project detection, runtime/dependency/service state, drift, and evidence-based failure diagnosis.",
  "main": "src/index.ts",
  "type": "module",
  "license": "Apache-2.0",
  "dependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "verify": "node scripts/verify-compliance.mjs",
    "smoke": "node scripts/smoke-test.mjs",
    "regress": "node scripts/regression-test.mjs",
    "test": "npm run typecheck && npm run verify && npm run regress && npm run smoke"
  }
}
